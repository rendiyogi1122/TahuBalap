[build]
buildCommand = "composer install --no-dev && npm run build && php artisan config:cache && php artisan event:cache && php artisan route:cache"

[deploy]
startCommand = "php artisan view:cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"